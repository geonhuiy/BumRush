using UnityEngine;

/*public class GameManager : MonoBehaviour

{
    public static GameManager gManagerInstance;
    private void Awake()
	{
        if (gManagerInstance == null)
		{
            gManagerInstance = this;
		}

        else
		{
            Destroy(this);
		}
        DontDestroyOnLoad(this);
	}
    public void EndGame()
     {
        Debug.Log("Game Over");*/
   